(ns ttt-clojure.gui-spec
  (:require  [speclj.core :refer :all]
            [ttt-clojure.gui :as sut]
            ))
;(describe "GUI tests"
;
; (it "tests draw state"
;    (should= 1 (sut/draw-state )))
;)
